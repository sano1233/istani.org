{
  "name": "istani",
  "version": "1.0.0",
  "description": "Static site for ISTANI Fitness",
  "private": true,
  "scripts": {
    "build": "echo 'No build step required'",
    "test": "echo 'No tests specified' && exit 0"
  }
}
